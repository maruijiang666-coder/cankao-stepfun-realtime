version: '3.8'

services:
  backend:
    build: ./backend
    container_name: mofan-realtime-backend
    ports:
      - "8080:8080"
    environment:
      - API_KEY=${API_KEY}
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8080 --reload
